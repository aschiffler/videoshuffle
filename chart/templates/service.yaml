apiVersion: v1
kind: Service
metadata:
  name: {{ include "videoshuffle.fullname" . }}
  labels:
    {{- include "videoshuffle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "videoshuffle.selectorLabels" . | nindent 4 }}